/**
 * Branding plugin placeholder for Sanity Studio.
 * Custom styling is now loaded globally in the Studio page.
 */
export function brandingPlugin() {
  return {
    name: 'ibra-global-english-branding',
  };
}
